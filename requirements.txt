# CupGet — Python dependencies
# Pin major versions for reproducibility; allow patch updates.

# ── Web framework ─────────────────────────────────────────────
fastapi>=0.111.0,<0.200.0
uvicorn[standard]>=0.29.0,<0.30.0

# ── Data validation ───────────────────────────────────────────
pydantic>=2.7.0,<3.0.0

# ── Video extraction engine ───────────────────────────────────
yt-dlp>=2024.5.1

# ── HTTP client (used internally by yt-dlp & optional routes) ─
httpx>=0.27.0,<0.28.0

# ── Async support ─────────────────────────────────────────────
anyio>=4.3.0,<5.0.0

httpx
